<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Bienvenue à Sekolintsika{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 128 128'><text y='1.2em' font-size='96'>⚫️</text><text y='1.3em' x='0.2em' font-size='76' fill='%23fff'>sf</text></svg>">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">

    <!-- Google Fonts (optionnel) -->
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Nunito', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .navbar-brand {
            font-weight: bold;
            letter-spacing: 1px;
        }

        .nav-link:hover {
            color: #ffc107 !important;
            transition: 0.3s;
        }

        footer {
            background-color: #212529;
        }
    </style>

    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="#">Sekolintsika</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a class="nav-link{% if app.request.attributes.get('_route') == 'app_public_acceuil' %} active{% endif %}" href="{{ path('app_public_acceuil') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if app.request.attributes.get('_route') == 'app_public_a_propos' %} active{% endif %}" href="{{ path('app_public_a_propos') }}">À propos</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if app.request.attributes.get('_route') == 'app_public_contact' %} active{% endif %}" href="{{path('app_public_contact')}}">Contact</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if app.request.attributes.get('_route') == 'app_register' %} active{% endif %}" href="{{ path('app_register') }}">Inscription</a>
            </li>
            <!-- Plus de liens ici -->
            </ul>
        </div>
    </div>
</nav>


    <!-- Main content -->
    <main class="container my-5 flex-grow-1">
        {% block body %}
        <div class="text-center py-5">
            <h1 class="display-5">Bienvenue sur Sekolintsika</h1>
            <p class="lead text-muted">Votre plateforme pour une éducation meilleure.</p>
        </div>
        {% endblock %}
    </main>

    <!-- Footer -->
    <footer class="text-white py-3 mt-auto">
        <div class="container-fluid text-center">
            <small>&copy; {{ "now"|date("Y") }} Sekolintsika. Tous droits réservés.</small>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
